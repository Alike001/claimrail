# All defaults target Somnia Shannon testnet and are read-only.
RPC_URL=https://api.infra.testnet.somnia.network
WS_RPC_URL=wss://api.infra.testnet.somnia.network/ws
INDEXER_URL=https://dev.smk.somnia.host/v1/graphql

# Optional public wallet to inspect. No private key is accepted by this probe.
WALLET_ADDRESS=
